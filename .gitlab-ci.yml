before_script:
  - apt-get update -qq && apt-get install -y -qq cmake
  - git clone --recursive https://github.com/Snaipe/Criterion
  - mkdir build
  - cd build
  - cmake ..
  - cmake --build .

test:
    script:
      - make test_run -C my
